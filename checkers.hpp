#pragma once

#include "Server.hpp"

/* 
DO I NEED  separate header for that?????????
cannot I add it to server?
*/
void check(int argc, char **argv); //Can't use const char** ! Not safe, pointer problem